{% doc %}
  @prompt
    Crea una sección de blog con diseño split-screen (pantalla dividida). La columna izquierda ocupa 1/3 del ancho, tiene fondo personalizable y contiene el título y la fecha alineados a la izquierda con tipografía serif elegante. La columna derecha ocupa 2/3, mostrando una imagen en formato landscape (3:2). Aplica un border-radius de 20px a ambos contenedores para redondear las esquinas y añade un espaciado (gap) de 24px entre columnas. El estilo debe ser minimalista, limpio y estéticamente premium.
{% enddoc %}
{% assign ai_gen_id = block.id | replace: '_', '' | downcase %}

{% style %}
  .ai-blog-split-{{ ai_gen_id }} {
    display: block;
    width: 100%;
  }

  .ai-blog-split__container-{{ ai_gen_id }} {
    display: flex;
    gap: 24px;
    width: 100%;
    align-items: stretch;
  }

  .ai-blog-split__left-{{ ai_gen_id }} {
    flex: 0 0 calc(33.333% - 12px);
    background-color: {{ block.settings.left_background_color }};
    border-radius: 20px;
    padding: 40px;
    display: flex;
    flex-direction: column;
    justify-content: center;
    align-items: flex-start;
  }

  .ai-blog-split__title-{{ ai_gen_id }} {
    font-family: Georgia, 'Times New Roman', serif;
    font-size: {{ block.settings.title_size }}px;
    color: {{ block.settings.text_color }};
    margin: 0 0 16px 0;
    line-height: 1.2;
    font-weight: 400;
  }

  .ai-blog-split__date-{{ ai_gen_id }} {
    font-family: Georgia, 'Times New Roman', serif;
    font-size: {{ block.settings.date_size }}px;
    color: {{ block.settings.text_color }};
    opacity: 0.7;
    margin: 0;
  }

  .ai-blog-split__right-{{ ai_gen_id }} {
    flex: 0 0 calc(66.667% - 12px);
    border-radius: 20px;
    overflow: hidden;
    position: relative;
  }

  .ai-blog-split__image-wrapper-{{ ai_gen_id }} {
    width: 100%;
    height: 100%;
    min-height: 400px;
  }

  .ai-blog-split__image-{{ ai_gen_id }} {
    width: 100%;
    height: 100%;
    object-fit: cover;
    display: block;
  }

  .ai-blog-split__placeholder-{{ ai_gen_id }} {
    width: 100%;
    height: 100%;
    min-height: 400px;
    background-color: #f4f4f4;
    display: flex;
    align-items: center;
    justify-content: center;
    position: relative;
  }

  .ai-blog-split__placeholder-{{ ai_gen_id }} svg {
    width: 100%;
    height: 100%;
    max-width: 300px;
    max-height: 300px;
    opacity: 0.3;
  }

  .ai-blog-split__empty-state-{{ ai_gen_id }} {
    position: absolute;
    bottom: 20px;
    left: 50%;
    transform: translateX(-50%);
    padding: 12px 20px;
    font-size: 14px;
    color: #666;
    text-align: center;
    pointer-events: none;
  }

  @media screen and (max-width: 768px) {
    .ai-blog-split__container-{{ ai_gen_id }} {
      flex-direction: column;
      gap: 16px;
    }

    .ai-blog-split__left-{{ ai_gen_id }},
    .ai-blog-split__right-{{ ai_gen_id }} {
      flex: 0 0 100%;
    }

    .ai-blog-split__left-{{ ai_gen_id }} {
      padding: 30px;
    }

    .ai-blog-split__image-wrapper-{{ ai_gen_id }},
    .ai-blog-split__placeholder-{{ ai_gen_id }} {
      min-height: 300px;
    }
  }
{% endstyle %}

<div class="ai-blog-split-{{ ai_gen_id }}" {{ block.shopify_attributes }}>
  <div class="ai-blog-split__container-{{ ai_gen_id }}">
    <div class="ai-blog-split__left-{{ ai_gen_id }}">
      {% if block.settings.title != blank %}
        <h2 class="ai-blog-split__title-{{ ai_gen_id }}">{{ block.settings.title }}</h2>
      {% endif %}
      {% if block.settings.date != blank %}
        <p class="ai-blog-split__date-{{ ai_gen_id }}">{{ block.settings.date }}</p>
      {% endif %}
    </div>

    <div class="ai-blog-split__right-{{ ai_gen_id }}">
      {% if block.settings.image %}
        <div class="ai-blog-split__image-wrapper-{{ ai_gen_id }}">
          <img
            src="{{ block.settings.image | image_url: width: 1200 }}"
            alt="{{ block.settings.image.alt | escape }}"
            loading="lazy"
            width="{{ block.settings.image.width }}"
            height="{{ block.settings.image.height }}"
            class="ai-blog-split__image-{{ ai_gen_id }}"
          >
        </div>
      {% else %}
        <div class="ai-blog-split__placeholder-{{ ai_gen_id }}">
          {{ 'image' | placeholder_svg_tag }}
          <div class="ai-blog-split__empty-state-{{ ai_gen_id }}">
            Añade una imagen para completar el diseño
          </div>
        </div>
      {% endif %}
    </div>
  </div>
</div>

{% schema %}
{
  "name": "Blog split screen",
  "tag": null,
  "settings": [
    {
      "type": "header",
      "content": "Contenido"
    },
    {
      "type": "text",
      "id": "title",
      "label": "Título",
      "default": "Explorando nuevas tendencias"
    },
    {
      "type": "text",
      "id": "date",
      "label": "Fecha",
      "default": "28 de enero, 2026"
    },
    {
      "type": "image_picker",
      "id": "image",
      "label": "Imagen"
    },
    {
      "type": "header",
      "content": "Estilo de texto"
    },
    {
      "type": "range",
      "id": "title_size",
      "min": 20,
      "max": 60,
      "step": 2,
      "unit": "px",
      "label": "Tamaño del título",
      "default": 32
    },
    {
      "type": "range",
      "id": "date_size",
      "min": 12,
      "max": 24,
      "step": 1,
      "unit": "px",
      "label": "Tamaño de la fecha",
      "default": 16
    },
    {
      "type": "color",
      "id": "text_color",
      "label": "Color del texto",
      "default": "#371e54"
    },
    {
      "type": "header",
      "content": "Fondo"
    },
    {
      "type": "color",
      "id": "left_background_color",
      "label": "Color de fondo izquierdo",
      "default": "#efebe5"
    }
  ],
  "presets": [
    {
      "name": "Blog split screen"
    }
  ]
}
{% endschema %}